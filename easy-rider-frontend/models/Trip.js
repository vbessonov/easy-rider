export default class Trip {
  constructor(destination, startDate, endDate, comment) {
    this.destination = destination
    this.startDate = startDate
    this.endDate = endDate
    this.comment = comment
  }
}
